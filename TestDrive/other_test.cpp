/* For arbitrary tests */

#include "tests.h"

TEST(Misc, Other)
{
	/*for (int i = 0; i < SQ_N; i++)
	{
		cout << "Forward: " << Board::forward_sq(i, W) << endl;
		cout << "Backward: " << Board::backward_sq(i, W) << endl;
	}*/
}